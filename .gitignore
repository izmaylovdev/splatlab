# --- Python ---
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# Virtual environments
.venv/
venv/
env/

# --- Runtime project data (checkpoints, photos, colmap, snapshots) ---
# User-generated capture/training data — large binaries, not source.
/data/

# --- Editor / OS cruft ---
.DS_Store
Thumbs.db
.idea/
.vscode/

# --- Local Docker env (secrets: VAST_API_KEY, NGROK_AUTHTOKEN, S3 creds) ---
deploy/.env
# ngrok runtime files (the config holds the authtoken; log is noise)
deploy/.ngrok.yml
deploy/ngrok.log

# --- Claude Code ---
.claude/settings.local.json

# --- Misc ---
# Model checkpoints / point clouds if produced outside /data
*.pt
*.ply

# Temporal dev-server logs written by tests/test_workflow_e2e.py
local_server*.log
