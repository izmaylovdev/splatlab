# Core API service + worker
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9
pillow>=10.0
numpy>=1.26

# Durable orchestration + live telemetry bus (both API and worker)
temporalio>=1.7
redis>=5.0
# Async HTTP client for the Vast.ai GPU autoscaler (server/vast). Control plane
# only; not needed on the GPU boxes.
httpx>=0.27
# Object storage bridge between a remote GPU worker and the API.
# Only needed when SPLATLAB_STORAGE=s3 (MinIO / S3); harmless otherwise.
boto3>=1.34
# Bundled ffmpeg binary (no system install) — used to extract frames from an
# uploaded video into a photo set. Wheels ship for Win/Linux/macOS.
imageio-ffmpeg>=0.5

# Real training (needs an NVIDIA GPU + CUDA build of PyTorch).
# Install PyTorch first following https://pytorch.org/get-started/locally/
# e.g.:  pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
# then:  pip install gsplat
# gsplat compiles CUDA kernels on first import (needs a CUDA toolkit / MSVC on Windows,
# or use their prebuilt wheels: https://docs.gsplat.studio/main/installation.html)
#
# torch
# torchvision
# gsplat>=1.4

# Camera pose estimation. pycolmap ships prebuilt wheels for Win/Linux/macOS.
# Alternatively install the COLMAP CLI and it will be used automatically if found.
pycolmap>=3.10; python_version >= "3.9"
